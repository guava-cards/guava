mutation LoginUser($login: String!, $password: String!) {
  loginUser(input: { login: $login, password: $password }) {
    failureReason

    user {
      ...Me
    }

    authToken {
      ...LoginUser_authToken
    }
  }
}

fragment LoginUser_authToken on AuthToken {
  csrf
}
