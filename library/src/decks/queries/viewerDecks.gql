query ViewerDecks($first: Int = 15, $after: String) {
  viewer {
    decks(first: $first, after: $after) {
      edges {
        cursor
        node {
          ...DeckRow_deck
        }
      }

      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
